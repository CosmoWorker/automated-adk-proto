[project]
name = "adk-samp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.26.0",
    "litellm>=1.82.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
